{
  "name": "destroy-extension",
  "version": "0.1.2",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "character:export": "node scripts/export-character.mjs",
    "build": "tsc --noEmit && node scripts/build.mjs",
    "dev": "vite --host 127.0.0.1",
    "test": "vitest run",
    "check": "npm run test && npm run build",
    "package": "npm run build && node scripts/package.mjs",
    "fixture:build": "node scripts/build-fixture.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.0",
    "fflate": "^0.8.3",
    "jsdom": "^26.1.0",
    "prettier": "^3.9.8",
    "typescript": "^5.9.3",
    "vite": "^7.3.0",
    "vitest": "^4.1.11"
  }
}
